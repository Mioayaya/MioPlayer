import styled from "@emotion/styled";

export const MioIconDiv = styled.div`
  .icon {
    height: 20px;
    width: 20px;    
    color: red;
    vertical-align: -0.15em;
    fill: currentColor;
    overflow: hidden;
  }
`
